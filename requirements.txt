# LLM Inference Memory Estimation Tool - Dependencies
# Python 3.8+ required

# Core dependencies
streamlit>=1.28.0
transformers>=4.30.0
# CPU-only PyTorch (much smaller, ~200MB vs 4GB with CUDA)
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu

# Optional: For Jupyter Notebook support
# jupyter>=1.0.0
# notebook>=7.0.0
# ipywidgets>=8.0.0
